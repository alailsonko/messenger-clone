apiVersion: v1
kind: Service
metadata:
  name: service-messenger-clone-database
  labels:
    app: service-messenger-clone-database
spec:
  selector:
    app: service-messenger-clone-database
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432